-- Generated by render.py — bootstrap for DEMO_DYNAMICS_APP
USE ROLE SYSADMIN;

CREATE WAREHOUSE IF NOT EXISTS DEMO_WH
    WAREHOUSE_SIZE = XSMALL AUTO_SUSPEND = 60 AUTO_RESUME = TRUE INITIALLY_SUSPENDED = TRUE
    COMMENT = 'Warehouse for DEMO_DYNAMICS_APP demo + Streamlit';

CREATE DATABASE IF NOT EXISTS DEMO_DYNAMICS_APP COMMENT = 'Cortex Dashboard Kit demo — synthetic data';
USE DATABASE DEMO_DYNAMICS_APP;
CREATE SCHEMA IF NOT EXISTS PUBLIC;
USE SCHEMA DEMO_DYNAMICS_APP.PUBLIC;
USE WAREHOUSE DEMO_WH;

CREATE STAGE IF NOT EXISTS DEMO_SEED_STAGE
    FILE_FORMAT = (TYPE = CSV FIELD_OPTIONALLY_ENCLOSED_BY = '"' SKIP_HEADER = 1 NULL_IF = ('', 'NULL', 'null'))
    COMMENT = 'Seed CSV landing zone';
CREATE STAGE IF NOT EXISTS STREAMLIT_STAGE COMMENT = 'Streamlit app artifacts';

SELECT CURRENT_ROLE() AS role, CURRENT_DATABASE() AS db, CURRENT_WAREHOUSE() AS wh;
