# Local tooling for the demo (the offline Bronze seeder for the 7ptrial path).
# NOTE: pandas/altair are NOT needed locally — they run inside Snowflake's
# Streamlit runtime. The seeder engine (src/seeders/_seedlib.py) uses the stdlib
# + Faker only.
faker>=20
